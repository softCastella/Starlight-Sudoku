# 2026-09-10 후속 작업 (일시정지·다시 풀기 모달)

선행: `Docs/Meeting/2026-09-10_작업.md`. 회귀: `Docs/Harness/검증.md`. APK는 `main`.

**상태: 구현·푸시 완료** (앱 `b4431b9`+, 웹 WebDemo `c911233`+). 설정 방침 `?lang=` 포함.  
**다음(집에서):** Play 입점 — `Docs/Meeting/2026-09-10_Play입점_후속.md`.

## 구현된 것

### 일시정지

- AppBar 일시정지 → 보드·입력 차단 오버레이 + 양피지 「일시 정지」+「계속 풀기」
- 정지 중 아이콘 ▶. 타이머 `isPaused`로 멈춤
- 파일: `pause_puzzle_overlay.dart`, `game_screen.dart`

### 다시 풀기

- AppBar 새로고침 → 확인 양피지 후 `giveUp()` 리셋. 「계속 풀기」면 유지
- 파일: `retry_puzzle_dialog.dart`

### 공통

- 어떤 모달이 떠 있어도 타이머 정지(`timer_widget` + `ModalRoute.isCurrent`)
- 방침 링크 UI 언어 `?lang=` 항상 전달
- WebDemo에 동일 반영·Pages 배포됨

## (옛) 할 일 — 참고용, 완료

아래는 구현 전 스펙. 회귀 시 `Docs/Harness/검증.md` 퍼즐 절을 본다.

1. 일시정지 오버레이·재개
2. 다시 풀기 확인 모달
3. 실기기·웹 확인
4. 하네스 항목 추가

## 하지 말 것

- 확인 없이 보드 리셋으로 되돌리기
- 다시 풀기만 Material `AlertDialog`로 두기
- `sample-v1` 건드림, 앱 `pages`를 `main`에 병합
