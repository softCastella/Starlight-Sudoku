# 2026-09-09 작업 (타원·모달)

시작 전: `Docs/Harness/검증.md`. APK는 `main`. 웹 공개는 `pages` 푸시만. `sample-v1`은 읽기 전용.

확정 값 표: `Docs/Harness/검증.md` **2026-09-09 확정 값**. 아침 메모: `Docs/Meeting/2026-09-09_후속작업.md`.

## 오늘 한 일

갤럭시 S25(`SM-S931N` / `R3CY50FP0QD`)에 로컬 릴리스 APK를 덮어가며 맞췄다. Quest 안 넣음. `pages` 안 올림. 스토어 빌드(`STORE_BUILD`) 없음.

### 글자

- 타원·모달 버튼 **11**. 타이틀 두루마리만 **15**. 공통 11로 타이틀을 덮지 않음
- 크레딧: 제목 18, 게임명 14, 본문 12, 저작권 11. 창 비율 **0.98**, 제목 위 12%
- 설정 양피지 = 크레딧 창 규격. 스위치 레이아웃은 그대로

### 타원 버튼 (닫기 = 기본)

짧은 「닫기」「다음」「미션」이 기준이다. 높이 ≈ 25.6, 폭 **80**, 글자 11, 좌우 별 여백 15.2(끝 비율 0.19).

긴 글(「첫 창문을 밝히기」, `Keep playing`, Missions 등)은 **가운데 몸통만** 가로로 늘린다. 별 끝 크기는 짧은 버튼과 같다. 좌·중·우 세 장(`_OvalMiddleStretch`).

- **한다:** 인트로, 마을 미션, 모든 모달 타원
- **안 한다:** 그림 전체 `BoxFit.fill`. `Image.centerSlice`(스키니 타원에서 그림이 사라짐). `FittedBox`/`Expanded`로 버튼을 창에 맞추거나 반씩 늘리기
- BGM 타원은 한 장 그대로(칩 폭 슬라이더)

### 양피지 창

버튼과 같은 방식. 모서리 말은 고정. 가로는 빨간 띠, 세로는 파란 띠만 늘림(`_ParchmentNineSlice`). `Image.centerSlice` 쓰지 않음.

안쪽 패딩 **40**. 타원 글자 여백(15.2)보다 넓다. 기준은 크레딧 창.

- 크레딧·설정: 비율 0.98 유지
- 종료·나갈까요·클리어·체험판: **글 + 패딩만큼** 높이. 화면 끝까지 늘리지 않음

### 타이틀 두루마리

한만 폭 베이크 208.86. 클러스터는 `alignY` 0.79에서 두루마리 높이의 3/8만큼 내림.

### 편집 JSON

스키마 3. 저장 JSON은 **locales가 앞**. `play_ui_baked.dart`에 2026-09-09 폰 폭·높이 배율. 저장 시 폰 `Android/data/com.tychespark.starlightsudoku/files/play_ui_tune.json`.

## APK

`flutter build apk --release` (STORE_BUILD 없음) → `SM-S931N` (`R3CY50FP0QD`) `install -r` Success.

## 하지 말 것

- 타원·양피지에 `Image.centerSlice`
- 짧은 확인 창을 화면 높이까지 늘리기
- BGM·타이틀 두루마리에 인트로 3조각 넣기
- `sample-v1` 체크아웃·커밋·머지
- `pages` 푸시, Quest 설치, Play 콘솔

## 열린 일

- UI 편집 화면 360×640 미리보기는 실기기와 다를 수 있음. 비율은 실기기·크레딧 창을 본다
- 제작자 웹 `UI_TUNER` 등: `Docs/Meeting/2026-09-08_UI모달_편집도구_후속작업.md`
- Play 콘솔·키스토어는 아직
